openapi: 3.2.0
info:
  title: University Of Washington Resource List API
  version: '1.0'
  description: 'Operations tagged ResourceList across 2 of this provider''s published API definitions: university-of-washington-idcard-web-service.yaml, university-of-washington-student-web-service.yaml. Each path carries the servers of the definition it was published in.'
servers:
- url: /idcard
- url: /student
tags:
- name: ResourceList
paths:
  /v1:
    get:
      tags:
      - ResourceList
      operationId: GetResources
      responses:
        '200':
          description: OK
          content:
            text/html:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel'
            application/xml:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel'
            text/xml:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel'
            text/plain:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel'
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel'
            text/json:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel'
        '202':
          description: Accepted
        '301':
          description: MovedPermanently
        '302':
          description: Found
        '304':
          description: NotModified
        '401':
          description: Identity '{identity}' is not authorized to access this resource.
        '405':
          description: MethodNotAllowed
        '500':
          description: InternalServerError
        '503':
          description: ServiceUnavailable
      summary: Get resources
      x-summary-source: derived
    servers:
    - url: /idcard
  /v5:
    get:
      tags:
      - ResourceList
      summary: Get available v5 resources
      description: '{

        "DisplayTitle" : "Service Resource List",

        "ews_guid" : "4533A5E5-2C26-4923-8574-A6C07FFFE51D",

        "LongDescription" : "Get available resources",

        "ShortDescription" : "Get available resources",

        "OriginatingSchema" : [ { "source": "hardcoded" } ],

        "Notes" : "This resource requires any of the following ASTRA roles:

        - sws:Support

        - sws:GradeSubmitter

        - sws:UnitReader

        - sws:AdmReader

        This resource supports the following authentication types:

        - X.509 Certificate

        - NetID

        - AccessToken ",

        "Links" : [],

        "Synonyms" : []

        }'
      operationId: GetSWSResources
      responses:
        '200':
          description: OK
          content:
            text/html:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel_2'
            application/xml:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel_2'
            text/xml:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel_2'
            text/plain:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel_2'
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel_2'
            text/json:
              schema:
                $ref: '#/components/schemas/ResourceListViewModel_2'
        '202':
          description: Accepted
        '301':
          description: MovedPermanently
        '302':
          description: Found
        '304':
          description: NotModified
        '401':
          description: Identity '{identity}' is not authorized to access this resource.
        '405':
          description: MethodNotAllowed
        '500':
          description: InternalServerError
        '503':
          description: ServiceUnavailable
    servers:
    - url: /student
components:
  schemas:
    UriResourceWithTitle:
      type: object
      properties:
        href:
          type:
          - string
          - 'null'
          format: uri
        name:
          type:
          - string
          - 'null'
      additionalProperties: false
    ResourceListViewModel:
      type: object
      properties:
        metadata:
          type:
          - string
          - 'null'
        pageCount:
          type:
          - string
          - 'null'
          description: 'Actual number of records on the current search results page. This must be set by the view in order

            for paging to work.'
        previousPageStart:
          type:
          - string
          - 'null'
          description: The counter value for the start of the previous search results page.
          readOnly: true
        pageSize:
          type:
          - string
          - 'null'
          description: Requested number of records for the current search results page.
        pageStart:
          type:
          - string
          - 'null'
          description: The counter value for the start of the current search results page.
        nextPageStart:
          type:
          - string
          - 'null'
          description: The counter value for the start of the next search results page.
          readOnly: true
        totalCount:
          type: integer
          format: int32
        pageStartOffset:
          type: integer
          description: 'The page start offset. Since sources are generally 0 based and the view

            defaults to 1 based, our offset defaults to -1.'
          format: int32
        format:
          $ref: '#/components/schemas/ViewFormats'
        xhtmlTitle:
          type:
          - string
          - 'null'
          readOnly: true
        resources:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/UriResourceWithTitle'
      additionalProperties: false
    ViewFormats:
      enum:
      - 1
      - 2
      - 3
      - 5
      type: integer
      format: int32
    UriResourceWithTitle_2:
      type: object
      properties:
        Href:
          type:
          - string
          - 'null'
          format: uri
        Name:
          type:
          - string
          - 'null'
      additionalProperties: false
      description: "{\n    \"DisplayTitle\" : \"Resource URI\"\n}"
    ResourceListViewModel_2:
      type: object
      properties:
        Resources:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/UriResourceWithTitle_2'
      additionalProperties: false
      description: "{\n    \"DisplayTitle\" : \"Resource List Model\",\n    \"TargetSchema\" : [{\"target\" : \"SWS.Resource\"}]\n}"
x-refined-from:
- university-of-washington-idcard-web-service.yaml
- university-of-washington-student-web-service.yaml
x-operator: institution
x-provenance:
  method: searched
  source: https://iam-tools.u.washington.edu/apis/gws/api.yaml
  generated: '2026-08-30'
  note: Fetched verbatim from the UW-IT IAM tools API documentation host on 2026-08-30 (HTTP 200). servers[] resolve to University of Washington registrable domains (uw.edu / washington.edu), so the operator is the institution, not a vendor. Only externalDocs, x-operator and x-provenance were added; info and paths are as published.
  pristine_copy: openapi/_original/university-of-washington-groups-web-service-v3.yaml
